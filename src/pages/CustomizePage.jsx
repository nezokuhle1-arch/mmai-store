/* ============================================================
   MMAI — CUSTOMIZE PAGE
   The core differentiator: live garment customization engine.
   Phase 1: shell only. Built out fully in Phase 3.
   ============================================================ */

export default function CustomizePage() {
  return (
    <section style={{ padding: 'var(--space-xl) var(--gutter)' }}>
      <h2>Customize your piece</h2>
      <p style={{ marginTop: 'var(--space-sm)' }}>
        Name badge, stitched quote, fabric placement, and barcode ID —
        coming in Phase 3.
      </p>
    </section>
  );
}
