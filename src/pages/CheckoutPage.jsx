/* ============================================================
   MMAI — CHECKOUT PAGE
   Order review before Shopify hosted checkout handoff.
   Phase 1: shell only. Wired to Shopify in Phase 4.
   ============================================================ */

export default function CheckoutPage() {
  return (
    <section style={{ padding: 'var(--space-xl) var(--gutter)' }}>
      <h2>Checkout</h2>
      <p style={{ marginTop: 'var(--space-sm)' }}>
        Order review and Shopify checkout handoff — wired in Phase 4.
      </p>
    </section>
  );
}
